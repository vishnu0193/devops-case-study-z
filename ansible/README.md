# Ansible plays for installing docker,nginx,log rotation

# steps to execute locally.

1. ansible-playbook -i inventory play.yml

2. 3 seperate roles have been created for docker,nginx and log rotation

